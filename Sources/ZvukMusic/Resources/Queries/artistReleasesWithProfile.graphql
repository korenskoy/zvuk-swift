# Извлечено из web-клиента zvuk.com v8.6.2
query artistReleasesWithProfile($ids: [ID!]!, $limit: Int = 100, $cursor: String = null) {
  getArtists(ids: $ids) {
    id
    title
    image {
      src
    }
    discography {
      all(limit: $limit, cursor: $cursor) {
        releases {
          id
          title
          type
          image {
            src
          }
          artists {
            id
            title
          }
          tracks {
            id
            title
            artists {
              id
              title
            }
            duration
            explicit
            artistTemplate
            mark
          }
          artistTemplate
          date
          explicit
          mark
        }
        pageInfo: page_info {
          endCursor
          hasNextPage
        }
      }
    }
  }
}
