# Извлечено из web-клиента zvuk.com v8.6.2
query getSearchArtistIds($query: String, $limit: Int = 1) {
  search(query: $query) {
    artists(limit: $limit) {
      items {
        id
      }
    }
  }
}
