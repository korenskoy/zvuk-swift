# Извлечено из web-клиента zvuk.com v8.6.2
query getSearchPodcastIds($query: String, $limit: Int = 1) {
  search(query: $query) {
    podcasts(limit: $limit) {
      items {
        id
      }
    }
  }
}
