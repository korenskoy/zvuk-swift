# Извлечено из web-клиента zvuk.com v8.6.2
query getPopularSearches($cursor: PageCursor = null, $explicit: Boolean, $limit: PositiveInt) {
  popularSearches(explicit: $explicit, limit: $limit, cursor: $cursor) {
    cursor
    items {
      text
    }
  }
}
